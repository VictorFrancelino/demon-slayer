import { Button } from "@mui/material";
import { styled } from "@mui/system";

const ButtonDemonSlayer = styled(Button)(({ theme }) => ({
    backgroundColor: "#911714",
    color: "#FFFDFD",
    borderRadius: "25%",
    width: 50,
    height: 50,
    minWidth: 0,
    padding: 0,
    position: "absolute",
}))

export default ButtonDemonSlayer